---
description: Star Citizen game data sources - check these repos first
alwaysApply: true
---

# Star Citizen Data Sources

When needing Star Citizen game data (components, shops, mining, ships, etc.), check these sources **first** before web searching:

## Primary Data Source: Direct Game File Extraction

We extract data directly from Star Citizen's `Data.p4k` using StarBreaker.

**Local extracted data:** `extracted-data/libs/foundry/records/` (gitignored)

Key paths in extracted data:
- `crafting/blueprints/` - Blueprint definitions with crafting recipes
- `crafting/blueprintrewards/` - Mission → blueprint mappings
- `mining/mineableelements/` - Ore properties (instability, resistance, etc.)
- `entities/mineable/mineablerock_*` - Ship-mining RS scanner base signatures (`SSCSignatureSystemParams.signatures[4]`)
- `harvestable/providerpresets/` + `harvestable/clusteringpresets/` - Spawn weights and cluster odds
- `entities/scitem/ships/weapons/` - Mining lasers, weapons
- `entities/scitem/ships/{cooler,powerplant,shieldgenerator}/` - Ship components
- `crafting/qualityquantization/` + `crafting/qualitydistribution/` - Crafting quality bands
- `missionbroker/` - Mission broker entries (reputation missions)
- `reputation/standings/` - Standing thresholds
- `Data/ObjectContainers/PU/Shops/**/*.socpak` - Branded item shop layouts
- `Data/ObjectContainers/PU/loc/mod/**/reststop_*` - Rest-stop / refinery embedded shops
- `Data/Scripts/ShopInventories/` - Shop inventory + prices (when referenced by socpak)
- `Data/Localization/english/global.ini` - Game lore/flavor text (`items_commodities_*`, `item_desc*`, `Item_Desc_*`)

**Extraction scripts:**
- `scripts/extract-game-data.ps1` - Full DCB extract + localization; shop socpaks/ShopInventories with `-IncludeShopData` only
- `scripts/parse-extracted-data.mjs` - Parse extracted JSON into app data (includes `game-lore.json`)
- `scripts/lib/commodityLocalization.mjs` - Commodity key normalization for lore extraction
- `scripts/lib/gameLore.mjs` - Full lore extraction (commodities + item descriptions)

**Shop socpaks + ShopInventories** are extracted with `-IncludeShopData` into `extracted-data/` for future use (separate project — not bundled in this app).

## Secondary Data Sources

### 1. UEX Corp API (prices + shop locations)
**URL:** https://uexcorp.space/api/documentation

Crowdsourced. Always **baked** by a script — never called at runtime:

- Resource Tracker DFP Q0 bases — `npm run fetch-commodity-bases`
- Commodity Lookup buy/sell terminals — `npm run fetch-shop-data`
- Smart Cracker Advisor mining-gear buy locations — `npm run fetch-mining-gear-shops`

The gear bake is **mining heads, modules and gadgets only** (UEX categories 28/29/30, all flagged `is_mining`). Do not widen it to armour, ammo, food, personal weapons or ships — the Advisor is a mining loadout tool and must have no data to answer those. Join to game data on **Star Citizen uuid**; uuids are stripped before the Advisor prompt.

After refreshing gear shops, re-run `npm run copy-mining-advisor-catalog` and redeploy `mining-loadout-advisor`. Never ticker these refreshes.

### 2. Star Citizen Wiki API (General reference)
**URL:** https://api.star-citizen.wiki/api/

Community wiki API for general reference. **Archive lore comes from game localization**, not this API.

## Deprecated Sources

### MrKraken/StarStrings (DEPRECATED)
StarStrings has been replaced by direct game file extraction. Legacy scripts were removed from the repo.

### seneca0815-rgb/SC_Signature_Scanner (REFERENCE ONLY)
Community OCR overlay with a pre-built RS lookup table. **Not used by this repo** — ship-mining base RS values are extracted from `entities/mineable/` mineable rock entity defs (`SSCSignatureSystemParams.signatures[4]`). Useful for cross-checking in-game readings.

## Usage Pattern

1. **Game data (blueprints, components, mining, Archive lore, RS signatures):** Use extracted data or run extraction
2. **Commodity DFP bases:** UEX Corp API (`fetch-commodity-dfp-bases.mjs`)
3. Only web search if none of these sources have the data
