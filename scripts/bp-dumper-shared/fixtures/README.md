# Log fixtures

Small anonymized `Game.log` snippets used to keep Python and Store parsers aligned.

| File | Covers |
|---|---|
| `blueprint-award-sample.log` | Blueprint award line(s) for import/watch tests |

Add a new fixture whenever parse rules change; update both clients’ tests if present.
