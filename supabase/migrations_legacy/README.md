# Historical incremental migrations (001–041)

These SQL files record how the **production** Supabase database evolved over time.

**Do not run these on new projects.** Use the squashed baseline in [`../migrations/`](../migrations/) (`001`–`006`) instead.

If your database already ran 001–041, you are at end state — do not re-run the baseline.
